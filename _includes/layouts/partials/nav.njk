<header>
  {# <div class="pt-16 pb-8">
    <h1 class="home font-semibold text-3xl text-gray-800">
      <a href="{{ '/' | url }}">{{ metadata.title }}</a>
    </h1>
  </div> #}

  {#- Read more about `eleventy-navigation` at https://www.11ty.dev/docs/plugins/navigation/ #}
  <nav class="bg-white-800">
    <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
      <div class="relative flex items-center justify-between h-24">

        {# <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">
          <!-- Mobile menu button-->
          <button type="button" class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white" aria-controls="mobile-menu" aria-expanded="false">
            <span class="sr-only">Open main menu</span>
            <!--
              Icon when menu is closed.

              Heroicon name: outline/menu

              Menu open: "hidden", Menu closed: "block"
            -->
            <svg class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
            <!--
              Icon when menu is open.

              Heroicon name: outline/x

              Menu open: "block", Menu closed: "hidden"
            -->
            <svg class="hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div> #}



        <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">

          <div class="flex-shrink-0 flex items-center mt-2">
            <a href="/" aria-label="Pupbestie Home">
            <img class="block lg:hidden h-8 w-auto" src="assets/images/pupbestie-logo-alt.svg" alt="Pupbestie" inline>
            <img class="hidden lg:block h-8 w-auto" src="assets/images/pupbestie-logo-alt.svg" alt="Pupbestie" inline>
            </a>
          </div>

          <div class="flex-shrink-0 flex items-center ml-auto font-bold">
            {{ metadata.phone }}
          </div>


          <!--
          Nav items
          -->
          {# <div class="hidden sm:block sm:ml-auto">
            <ul class="flex flex-wrap text-xl space-x-4">
            {%- for entry in collections.all | eleventyNavigation %}
              <li class="text-gray-700 hover:bg-gray-100 hover:text-white px-3 py-2 rounded-md text-sm font-medium nav-item{% if entry.url == page.url %} bg-gray-100 nav-item-active{% endif %}">
                <!-- <li class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium mr-6 nav-item{% if entry.url == page.url %} nav-item-active{% endif %}"> -->
                <!-- <a class="text-gray-700 border-none hover:no-underline hover:text-gray-800" href="{{ entry.url | url }}"> -->
                <a class="text-gray-700 border-none hover:no-underline {% if entry.url == page.url %} text-white {% endif %}" href="{{ entry.url | url }}">
                  {{ entry.title }}
                </a>
              </li>
            {%- endfor %}
            </ul>
          </div> #}


        </div>

      </div>
    </div>
  </nav>
</header>
